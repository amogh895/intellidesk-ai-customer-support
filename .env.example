# Google Gemini Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# MongoDB Atlas Cloud Database
# Replace <db_password> with your actual database user password
MONGO_URI=mongodb+srv://admin_NorthBridgeAssurance:<db_password>@cluster0.1b0zm6r.mongodb.net/intellidesk_db?retryWrites=true&w=majority&appName=Cluster0

# JWT Authentication Secret (change in production)
JWT_SECRET=your_jwt_secret_key_here

# Application Configuration
BACKEND_PORT=8000
HOST=0.0.0.0

# Copilot Autonomy — true = Copilot auto-handles safe customer replies (less HITL)
# Escalations and high-risk actions still require human approval
COPILOT_AUTONOMY=true

# Vector Database Path
CHROMA_DB_PATH=chroma_db
